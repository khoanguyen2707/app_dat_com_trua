# deps
node_modules/
# build
dist/
server/dist/
web/dist/
server/src/generated/
# env (bỏ qua mọi file .env, kể cả .env.example)
.env
.env.*
# logs
*.log
*.tsbuildinfo
# os
.DS_Store
Thumbs.db
# docs (tài liệu nội bộ — không đẩy lên git)
docs/
# file tạm / nháp (không commit)
tmp-test-parse.*
*.tmp
